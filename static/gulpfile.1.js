/*
This gulpfile is valid but does nothing, it serves as our base

Required commands:
	npm install --global gulp-cli
	npm init
	npm install --save-dev gulp
 */

'use strict';

const gulp = require('gulp');

gulp.task('default', function() {
  // place code for your default task here
});